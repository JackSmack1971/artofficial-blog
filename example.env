# Next.js Development Configuration
NODE_ENV=development
NEXT_TELEMETRY_DISABLED=1
FORCE_COLOR=1

# Package Manager Preferences  
NPM_CONFIG_PREFER_OFFLINE=true
NPM_CONFIG_AUDIT_LEVEL=moderate
PNPM_HOME=/usr/local/bin

# Development Server Configuration
NEXT_SHARP=1
NEXT_CACHE_DIR=/tmp/.next

# Testing Configuration
CI=true
CYPRESS_CACHE_FOLDER=/tmp/cypress
JEST_WORKER_ID=1

# Performance Monitoring
LIGHTHOUSE_CI=1
BUNDLE_ANALYZE=0

# Proxy Configuration (for external API testing)
NODE_TLS_REJECT_UNAUTHORIZED=0

# Ghost CMS Integration (Setup-only access)
GHOST_API_URL=https://your-ghost-instance.ghost.io
GHOST_CONTENT_API_KEY=your_content_api_key_here
GHOST_ADMIN_API_KEY=your_admin_api_key_here

# Newsletter Service (Fallback option)
CONVERTKIT_API_KEY=your_convertkit_api_key_here

# Error Tracking and Monitoring
SENTRY_DSN=your_sentry_dsn_here
SENTRY_AUTH_TOKEN=your_sentry_auth_token_here

# Analytics (for testing purposes)
NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX
NEXT_PUBLIC_PLAUSIBLE_DOMAIN=artofficialintelligence.academy

# Development Authentication
NEXTAUTH_SECRET=your_nextauth_secret_here
NEXTAUTH_URL=http://localhost:3000

# Persistent cache directories for faster rebuilds
PNPM_CACHE_DIR=/tmp/.pnpm-cache
CYPRESS_CACHE_FOLDER=/tmp/cypress-cache
JEST_CACHE_DIRECTORY=/tmp/jest-cache
ESLINT_CACHE_LOCATION=/tmp/eslint-cache
NEXT_CACHE_DIR=/tmp/.next-cache

# --- ADR-aligned Required Environment (from docs/architecture-decisions.json:env_config.required) ---
# Ghost CMS Integration
GHOST_API_URL=https://your-ghost-instance.ghost.io
GHOST_CONTENT_API_KEY=your_content_api_key_here
GHOST_ADMIN_API_KEY=your_admin_api_key_here

# Newsletter Service (Fallback option)
CONVERTKIT_API_KEY=your_convertkit_api_key_here

# Analytics
NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX
NEXT_PUBLIC_PLAUSIBLE_DOMAIN=artofficialintelligence.academy

# Error Tracking
SENTRY_DSN=your_sentry_dsn_here

# Auth (NextAuth)
NEXTAUTH_SECRET=your_nextauth_secret_here
NEXTAUTH_URL=http://localhost:3000

# --- Development & Performance Toggles / Benchmarks ---
# Performance targets (used by CI scripts to assert benchmarks)
PERF_LIGHTHOUSE_MIN_SCORE=95
PERF_FCP_P95_TARGET_MS=1500
PERF_LCP_P95_TARGET_MS=2000
PERF_TTI_P95_TARGET_MS=3000

# Enable/disable dev-time instrumentation
DEV_TELEMETRY_ENABLED=1
DEV_METRICS_LOG_LEVEL=info

# Bundle analysis toggle (0=off, 1=on)
BUNDLE_ANALYZE=0

# Lighthouse CI toggle
LIGHTHOUSE_CI=1

# --- Tooling & Caching for faster local DX ---
NODE_ENV=development
NEXT_TELEMETRY_DISABLED=1
FORCE_COLOR=1

# Package Manager Preferences
NPM_CONFIG_PREFER_OFFLINE=true
NPM_CONFIG_AUDIT_LEVEL=moderate
PNPM_HOME=/usr/local/bin
PNPM_CACHE_DIR=/tmp/.pnpm-cache

# Development Server Configuration
NEXT_SHARP=1
NEXT_CACHE_DIR=/tmp/.next
NEXT_CACHE_DIR=/tmp/.next-cache

# Testing Configuration
CI=true
CYPRESS_CACHE_FOLDER=/tmp/cypress-cache
JEST_WORKER_ID=1
JEST_CACHE_DIRECTORY=/tmp/jest-cache

# Lint cache
ESLINT_CACHE_LOCATION=/tmp/eslint-cache

# Proxy Configuration (for external API testing)
NODE_TLS_REJECT_UNAUTHORIZED=0

# Sentry token for releases (optional for CI)
SENTRY_AUTH_TOKEN=your_sentry_auth_token_here
